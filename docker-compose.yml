services:
  api:
    build:
      context: ./apps/api
    ports:
      - "8000:8080"
    environment:
      APP_ENV: dev
      APP_SECRET: local-dev-secret
      PORT: "8080"
      CORS_ALLOW_ORIGIN: '^https?://localhost(:[0-9]+)?$'
